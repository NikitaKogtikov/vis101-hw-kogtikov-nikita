1.1 Ќет ответа
1.2 d3 берет данные из файла json и на основе этих данных создает количество строк, которое равно количеству элементов в дата-сете.
2.1 я бы выбрал виджет типа range, чтобы отфильтровывать данные, отбрасыва€ значени€ выше или ниже порогового
3.1 ћожно сделать по году агрегацию, по другим компонентам нельз€, так как значени€ в основном по этим компонентам различны, а вот по годам любопытно было бы посмотреть, как измен€етс€ население, ¬¬ѕ и прочее. 
	я бы использовал check-box, дл€ выбора агрегации по годам
4.1 –аньше данные были за один год, year хранит данные за несколько лет в зависимости от года. “о есть, допустим, есть параметр ¬¬ѕ, так вот в дата сете будет массив объектов, каждый из которых соответсвует какому-либо 
	году и сожержит данные (такие как ¬¬ѕ) дл€ каждого из этих годов.
5.1 SVG дает больше возможностей. Ёто пространство, где можно рисовать, и элементы могут накладыватьс€ друг на друга. HTML же строго структурирует элементы, один идет после другого, один содержит другой. 
7.1 ѕоказать данные, обозначающие динамику, например ожирение у различных слоев население. ƒанные в чистом виде не дадут нагл€дного ответа о динамике
7.2 »нтерактивность
7.3 ÷елостность и скажение данных. Ќапример, синий среди красных более синим кажетс€, нежели среди черных. ¬ажен контекст.
7.4 ќтношение (a) сравнени€, (b) эквивалентности, 
7.5 ÷вет, форма.
7.6 –азмер, длина, ширина. 
